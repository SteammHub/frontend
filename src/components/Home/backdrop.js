import React from 'react';

const Backdrop = (Aside,closeSidebar) => {
    return (
    <div className="backdrop"
    // {Aside?"backdrop backdrop--open":
    // "backdrop"} onClick={closeSidebar}
    >
        
    </div>  );
}
 
export default Backdrop;